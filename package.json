{
  "name": "jet2-ptfs",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && vite preview",
    "deploy": "npm run build && wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply jet2-ptfs-db --remote"
  },
  "dependencies": {
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "react-router-dom": "^7.8.2"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.12.0",
    "@vitejs/plugin-react": "^5.0.2",
    "vite": "^7.1.3",
    "wrangler": "^4.35.0"
  }
}
